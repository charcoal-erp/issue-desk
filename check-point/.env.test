# Loaded by Vite in `test` mode, ahead of .env — keeps `npm test` away from any
# real content dir. Must match test.env.DATA_DIR in vite.config.ts.
DATA_DIR=/tmp/checkpoint-vitest-data
WATCH_FILES=false
